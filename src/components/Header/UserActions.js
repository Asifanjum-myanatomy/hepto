import React from 'react';

const UserActions = () => {
  return (
    <div className="user-actions">
      <button>Sign In</button>
      <button>Cart</button>
    </div>
  );
};

export default UserActions;